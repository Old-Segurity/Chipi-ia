# Versiones específicas que funcionan en Render
setuptools==69.0.3
cffi==1.16.0
wheel==0.43.0

# Dependencias principales
fastapi==0.104.1
uvicorn[standard]==0.24.0
requests==2.31.0
python-dotenv==1.0.0
gunicorn==21.2.0

# cryptography con versión compatible
cryptography==41.0.8  # Versión más estable

# Otras dependencias (opcional, puedes comentarlas temporalmente)
# Pillow==10.0.1
# python-multipart==0.0.6
